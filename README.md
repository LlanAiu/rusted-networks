# Networks w/ Rust

A custom implementation of an auto-diff engine to build neural networks.

For practice in reference to "Deep Learning" (2017).

In the current implementation, all data is expected to be held in a ```data/``` folder at the root level.